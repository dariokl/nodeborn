/* Base app shell theme using S0.2 color tokens. */
Screen {
    background: $surface-bg;
    color: $text-primary;
}

Header,
Footer {
    background: $surface-panel;
    color: $text-primary;
    border: tall $surface-border;
}
